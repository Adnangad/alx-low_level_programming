#include "main.h"
#include <stdio.h>

/**
 * mul - prints two numbers
 * @a:first number
 * @b:second number
 *
 * Return:multiplied no
 */
int mul(int a, int b)
{
	int result = a + b;

	return (result);
}
