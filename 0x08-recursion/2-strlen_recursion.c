#include "main.h"
#include "_putchar.h"
#include <string.h>

/**
 *  _strlen_recursion - prints strlength
 *  @s:string to be counted
 *
 *  Return:length
 */
int _strlen_recursion(char *s)
{
	int length = strlen(s);

	return (length);
}
