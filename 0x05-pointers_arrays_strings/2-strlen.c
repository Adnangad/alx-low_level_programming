#include "main.h"
#include <string.h>

/**
 * _strlen - prints length of string
 * @s:string to be det
 *
 * Return:Length of string
 */
int _strlen(char *s)
{
	int len = strlen(s);

	return (len);
}
